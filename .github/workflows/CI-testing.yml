#name the workflow
name: CI and Testing

#Trigger to run the workflolw
on:
  push:
    branches: master
  pull_request:
    branches: master

#job to run
jobs:
  #name of job
  build:
    #type of runner machine
    runs-on: ubuntu-latest

    #steps to run on the runner
    steps:
      #step-1: checkout the code from the repo (download repo on the runner)
      - uses: actions/checkout@v4

      #step-2: set up Node.js environment on runner
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      #step-3: Install the repo's dependencies from package.json
      - name: Install dependencies
        run: npm install

      #step-4: Run the unit and integration tests
      - name: Run Tests
        run: npm test
